import 'package:flutter/material.dart';

// --- DESIGN CONSTANTS ---
// Based on your Figma and design preferences

// Colors
const Color PrimaryAccent = Color(0xFFE76F51); // Rose Gold / Coral Accent
const Color LightPink = Color(0xFFF7D7D8); 
const Color LightBlue = Color(0xFFC8EAFF); 
const Color ErrorRed = Color(0xFFE71D36);
const Color TextColor = Color(0xFF333333);
const Color ActiveCard = Color(0xFFFFFFFF); // White background for active cards
const Color InactiveCard = Color(0xFFF5F5F5); // Light gray for inactive cards

// Gradient
const BackgroundGradient = LinearGradient(
  colors: [LightPink, LightBlue],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);